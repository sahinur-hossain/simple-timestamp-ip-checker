# simple-timestamp-ip-checker
This repository hosts a lightweight application that returns the current timestamp and the request origin IP. It includes Terraform automation, Docker image configuration, and deployment instructions.
